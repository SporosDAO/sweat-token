import { Components } from '@mui/material'

export const MuiTooltipConfig: Components['MuiTooltip'] = {
  styleOverrides: {
    tooltip: {
      padding: '10px',
      lineHeight: 1.5,
      textAlign: 'center'
    }
  }
}
